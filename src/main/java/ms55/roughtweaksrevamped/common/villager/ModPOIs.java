package ms55.roughtweaksrevamped.common.villager;

public class ModPOIs {

	//public static DeferredRegister<PoiType> POI_TYPES = DeferredRegister.create(ForgeRegistries.POI_TYPES, RoughTweaksRevamped.MODID);
	
	/*public static void registerPOIs() {
		try {
			ObfuscationReflectionHelper.findMethod(PoiType.class, "registerBlockStates", PoiType.class).invoke(null, null)
		} catch (InvocationTargetException | IllegalAccessException e) {
			e.printStackTrace();
		}
	}*/

	//public static final RegistryObject<PoiType> MEDIC_TYPE = POI_TYPES.register("medic_type",
	//	() -> new PoiType(ImmutableSet.copyOf(ModBlocks.MEDIC.get().getStateDefinition().getPossibleStates()), 1, 1));
}